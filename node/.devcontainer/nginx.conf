events {
  worker_connections 1024;
}

http {
  server {
    listen 80;

    # location / {
    #   root /usr/share/nginx/html;
    # }

    location / {
      proxy_pass http://devcontainer:3000/;
    }
  }
}