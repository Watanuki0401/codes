version: '3'

services:
  nodes:
    build:
      context: .
      dockerfile: Dockerfile.node
    volumes:
      - ../../../codes:/workspace/codes
    tty: true

  proxys:
    build:
      context: ../proxy
      dockerfile: Dockerfile.proxy
    ports:
      - 80:80