events {
    worker_connections 512;
}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
        }

        location /nodes/ {
            proxy_pass http://nodes:3000/;
        }
    }
}